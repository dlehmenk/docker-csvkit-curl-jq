Docker container with [csvkit](https://csvkit.readthedocs.io/en/latest/), [curl](https://curl.se/) and [jq](https://stedolan.github.io/jq/) based on ubuntu jammy.

Automatically built and pushed to dockerhub nightly.
